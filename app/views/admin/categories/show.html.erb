<div class="d-flex">
  <%= render "admin/shared/sidebar", categories: @categories %>
  <div class="sidebar-aware-container">
    <div class="container pt-4">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h3><%= @category.display_name %></h3>
        <%= link_to "+ Agregar Subasta", new_admin_product_path, class: "btn btn-primary btn-large" %>
      </div>
      <div class="product-grid">
        <% @category.products.each do |product| %>
          <%= render "shared/product_card", product: product, admin: true %>
        <% end %>
      </div>
    </div>
    <div class="ml-3 mt-5">
      <%= link_to "Editar categoria", edit_admin_category_path(@category), class: "btn btn-secondary" %>
      <%= link_to "Eliminar categoria", admin_category_path(@category), method: :delete, data: { confirm: "Esto eliminará todas las subastas en esta categoría. Estas seguro?" }, class: "btn btn-secondary ml-3" %>
    </div>
  </div>
</div>
